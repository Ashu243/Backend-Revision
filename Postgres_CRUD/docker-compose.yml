services:
  postgres:
    image: postgres:17
    container_name: postgresDB
    restart: unless-stopped
    environment:
      POSTGRES_USER: ashu
      POSTGRES_PASSWORD: nahi_btaunga
      POSTGRES_DB: ecommerce
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: